# CLAUDE.md

@AGENTS.md

**All project rules live in [`AGENTS.md`](AGENTS.md)** — the single source of truth for every AI
assistant (file placement and repo-root hygiene, package managers, testing and the coverage
gate, coding style, documentation rules, git workflow). Read it in full; do not re-add project
rules here. Everything below applies ONLY to Claude Code — operational refinements of rules
already defined in `AGENTS.md`.
